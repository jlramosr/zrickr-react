const initialAppState = {
  name: 'ERP',
  categoriesPath: 'section'
}

const app = (state = initialAppState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default app